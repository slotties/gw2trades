<div class="page-header">
    <h1>
        #if ($query)
            #springMessage("index.title.query")
        #else
            #springMessage("index.title.default")
        #end
    </h1>
</div>

<div class="row">
    <form method="GET">
        <div class="col-lg-6">
            <div class="input-group">
                <span class="input-group-addon">#springMessage("index.search.name.label")</span>
                <input name="name" type="text" class="form-control" placeholder="#springMessage("index.search.name.placeholder")" #if($query && $query.name)value="${escapetool.html($query.name)}"#end>
            </div>
        </div>
        <div class="col-lg-6">
            <button type="submit" class="btn btn-default">#springMessage("index.search.btn")</button>
        </div>
    </form>
</div>

#parse("pager.vm")

<table class="table table-striped table-bordered table-hover">
    <tr>
        <th>&nbsp;</th>
        <th>&nbsp;</th>
        <th colspan="2">#springMessage("index.cols.buy")</th>
        <th colspan="2">#springMessage("index.cols.sell")</th>
    </tr>
    <tr>
        <th>
            <a href="#orderLink("name")">#springMessage("index.cols.item")</a>
            #orderIcon('name')
        </th>
        <th>
            <a href="#orderLink("profit")">#springMessage("index.cols.profit")</a>
            #orderIcon('profit')
        </th>
        <th>
            <a href="#orderLink("highestBidder")">#springMessage("index.cols.maxBuyer")</a>
            #orderIcon('highestBidder')
        </th>
        <th>
            <a href="#orderLink("avgBidder")">#springMessage("index.cols.avgBuyer")</a>
            #orderIcon('avgBidder')
        </th>
        <th>
            <a href="#orderLink("lowestSeller")">#springMessage("index.cols.minSeller")</a>
            #orderIcon('lowestSeller')
        </th>
        <th>
            <a href="#orderLink("avgSeller")">#springMessage("index.cols.avgSeller")</a>
            #orderIcon('avgSeller')
        </th>
    </tr>
#foreach($stats in $listingStatistics)
    <tr>
        <td>
            <img src="${stats.item.iconUrl}" alt="Icon" title="${stats.item.name}" class="item-icon-mini">
            <a href="/${springMacroRequestContext.locale}/details/${stats.item.itemId}.html" class="rarity-${stats.item.rarity.toLowerCase()}">
                ${stats.item.name} #if($stats.item.level > 0)(${stats.item.level})#end
            </a>
        </td>
        <td>#price(${stats.profit})</td>
        <td>#price(${stats.buyStatistics.maxPrice})</td>
        <td>#price($mathtool.floor($stats.buyStatistics.average))</td>
        <td>#price(${stats.sellStatistics.minPrice})</td>
        <td>#price($mathtool.floor($stats.sellStatistics.average))</td>
    </tr>
#end
</table>

#parse("pager.vm")